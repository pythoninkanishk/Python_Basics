# 3. Can we have a set with 18 (int) and '18' (str) as a value in it? 
# Yes, we can have both 18 (int) and '18' (str) in a set as they are different types.

Set = {18, '18'}
print("Set contains:", Set)